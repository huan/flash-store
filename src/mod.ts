import { log }      from './config'
import { VERSION }  from './version'
import {
  FlashStore,
}                   from './flash-store'
import {
  FlashStoreSync,
}                   from './flash-store-sync'

export default FlashStore
export {
  log,
  FlashStoreSync,
  FlashStore,
  VERSION,
}
